import React from 'react'
import { Icon } from 'semantic-ui-react'

const IconExampleCircular = () => (
  <div>
    <Icon circular name='bell' />
    <Icon circular color='teal' name='heart' />
    <Icon circular inverted name='bug' />
    <Icon circular inverted color='teal' name='settings' />
  </div>
)

export default IconExampleCircular